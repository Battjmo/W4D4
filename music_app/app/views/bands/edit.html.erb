<section class="page">
  <div class="centered-box">

  <form action="<%= band_url %>" method="post">
    <input type="hidden" name="_method" value="patch">

    <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

    <label for="band_name">What's was that sickass band actually called? </label>
    <input type="text"
      name="band[name]"
      id="band_name"
      value="<%= @band.name %>">

      <input type="submit">
    <br>
